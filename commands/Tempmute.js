const discord = require("discord.js");
const ms = require("ms");
module.exports.run = async (bot, message, args) => {

    // !tempmute @gebruiker tijd 



}

module.exports.help = {
    name: "tempmute"
}